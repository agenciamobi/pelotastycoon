import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agenciamobi.pelotastycoon',
  appName: 'Pelotas Tycoon',
  webDir: 'dist',
};

export default config;
